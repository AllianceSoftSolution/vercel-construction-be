import { Request, Response, NextFunction } from "express";
declare const sanitizePrivilegedIdentityResponse: (req: Request, res: Response, next: NextFunction) => void;
export default sanitizePrivilegedIdentityResponse;
